build-job:
  stage: build
  script:
    - whoami apt-get install -y postgresql postgresql-client libpq-dev
    - whoami -u postgres psql -d dots

    - virtualenv venv
    - . venv/bin/activate 
    - pip3 install -r django-dots/requirements.txt


test-backend:
  stage: test
  script:
    - python3 django-dots/manage.py migrate
    - python3 django-dots/manage.py test


deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."